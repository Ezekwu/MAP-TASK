// import jwt from "jsonwebtoken";
// import { JWT_SECRET_KEY } from "@/config";

// export function generateJwt(uid: string) {
//   const payload = { uid };
//   const options = { expiresIn: "7d" };
//   return jwt.sign(payload, JWT_SECRET_KEY, options)

// }
